ADDITIONAL_INCLUDE_DIRS += -I..

# The unit under test is the theme's alert panel implementation (compiled
# into the theme bundle with ARC, so the tool must use it too).
t_AlertPanelScrolling_OBJC_FILES += ../NSAlert+Eau.m
t_AlertPanelScrolling_TOOL_LIBS += -lgnustep-gui -lgnustep-base -ldispatch

t_EauMenuWindowFilter_OBJC_FILES += ../EauMenuWindowFilter.m
t_EauMenuWindowFilter_TOOL_LIBS += -lX11

# The unit under test is the theme's NSFont category, which swizzles on load.
t_EauFontSubstitution_OBJC_FILES += ../NSFont+Eau.m
t_EauFontSubstitution_TOOL_LIBS += -lgnustep-gui -lgnustep-base

ADDITIONAL_OBJCFLAGS += -Wno-deprecated

t_ButtonSpaceKey_TOOL_LIBS += -lgnustep-gui -lgnustep-base

# The theme under test is loaded at run time with -GSTheme.
t_ProgressStripes_TOOL_LIBS += -lgnustep-gui -lgnustep-base
t_MenuOverflowScale_OBJC_FILES += ../EauMenuScrollManager.m
t_MenuOverflowScale_TOOL_LIBS += -lgnustep-gui -lgnustep-base
# Like the theme, the scroll manager is built with ARC
../EauMenuScrollManager.m_FILE_FLAGS += -fobjc-arc
t_GrowBoxInset_OBJC_FILES += ../EauGrowBoxView.m ../EauGrowBoxCell.m
t_GrowBoxInset_TOOL_LIBS += -lgnustep-gui -lgnustep-base
# Like the theme, the grow box is built with ARC
../EauGrowBoxView.m_FILE_FLAGS += -fobjc-arc
../EauGrowBoxCell.m_FILE_FLAGS += -fobjc-arc
